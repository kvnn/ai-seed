2026-03-26T21:03:06Z The reference frontend targets a broader crawlbuild API than this repo exposes, so I adapted it into a workshop-specific auth and run UI instead of copying broken routes.
2026-03-26T21:03:06Z docker-compose.yml pointed the API service at a non-existent ./backend path, so I aligned it with the current root Dockerfile and /app package layout.
2026-03-27T19:42:16Z The repo package was renamed from app to backend, but Python imports and container commands still referenced app, so I aligned the stack and added a public backend-root endpoint to verify the rename from the browser.
2026-03-27T19:42:16Z backend.main still could not import because backend.models only defined User while auth and run services expected InviteCode and Run, so I restored the minimal models needed for the current app to boot.
2026-03-27T21:32:04Z Local docker compose should not require a fixed backend host port because the frontend can proxy /api and /preview to the backend service over the docker network, so I removed the default backend host bind and kept only the frontend published port configurable.
2026-03-27T21:50:23Z The compose service should be named backend for operator clarity, and the local sync SQLAlchemy setup must rewrite asyncpg URLs to psycopg so the backend container can boot against the current env file.
2026-03-27T21:58:02Z Local compose should not default to a remote RDS credential from .env, so I overrode the backend DB URL to sqlite under /data and removed the frontend container's unnecessary inheritance of backend secrets.
2026-03-27T22:54:40Z The old src/services tree was orphaned after the app-to-backend rename, so I am folding useful modules into backend/apps for domain code and backend/services for cross-cutting adapters, with backend as the only canonical Python package root.
2026-03-27T23:05:12Z The moved brand, generation, Firecrawl, and S3 modules depend on optional packages not present in the current local env, so I am making those imports fail lazily with clear runtime errors instead of breaking backend imports at module load time.
2026-03-27T23:30:00Z The front page was an internal backend-handshake that proved the stack rename worked — useful for development, wrong for the seed's actual audience. Replaced it with a landing page that explains what the repo is and how to get started. The BackendRootResponse API is still available but no longer displayed on the public index.
2026-03-27T23:30:00Z Added docs/VISION.md as a guided template. The intent is that AGENTS.md will be updated to prompt users to fill this out on first Claude Code session, creating a living document that grounds all future conversations.
2026-03-28T19:16:11Z The highest public-repo exposure risk is still the ignored local `.env` plus the ignored `data/` directory; tracked source files do not currently appear to contain live secrets, but the starter should eventually ship with a redacted `.env.example` and a stricter JWT secret requirement.
